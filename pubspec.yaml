name: conest
description: "Cross-platform secure text exchange prototype."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cryptography: ^2.7.0
  cupertino_icons: ^1.0.9
  flutter_secure_storage: ^9.2.4
  mobile_scanner: ^6.0.7
  path_provider: ^2.1.5
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
